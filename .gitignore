# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — NEVER commit secrets
.env
.env*.local
.env.local

# vercel
.vercel

# curriculum importer — keep private source URLs + run reports out of git
/scripts/curriculum-sources.json
/scripts/import-report.json
# imported PDFs (public store). Remove this line if you use the `public` store
# on Vercel and need the files committed so they deploy with the app.
/public/resources/**/*.pdf

# typescript
*.tsbuildinfo
next-env.d.ts
